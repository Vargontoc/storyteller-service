VITE_API_BASE_URL=http://localhost:8080

# Request timeout for API calls (ms). Generation endpoints (story/page/image/audio) can take a while.
VITE_API_TIMEOUT_MS=180000

# Dev/preview server port. strictPort is on: if it's taken, the app refuses to start.
APP_PORT=80
